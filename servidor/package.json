{
  "name": "dvita-pdf-service",
  "version": "1.0.0",
  "description": "D'Vita PDF generation service for payment tickets",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "echo \"No tests configured yet\" && exit 0",
    "lint": "eslint ."
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "puppeteer": "^23.0.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}
